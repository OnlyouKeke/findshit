/**
 * 应用配置文件
 * 包含默认坐标、搜索参数等关键配置
 */

// 应用标题
export const APP_TITLE = '找厕所';

// 默认搜索半径（米）
export const DEFAULT_RADIUS_M = 5000;

// 默认结果数量限制
export const DEFAULT_LIMIT = 20;

// 默认地图中心点（北京）
export const DEFAULT_CENTER = {
  lat: 39.9098,
  lng: 116.3974
};

// 默认地图缩放级别
export const DEFAULT_ZOOM = 15;